{
  "name": "@yoursign/crypto",
  "version": "0.1.0",
  "description": "AES-256-GCM, X25519, Ed25519, and the canonical YourSign signing message. Sensitive — see CLAUDE.md.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint TODO'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@noble/ciphers": "^1.0.0",
    "@noble/curves": "^1.6.0",
    "@noble/ed25519": "^2.2.0",
    "@noble/hashes": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
