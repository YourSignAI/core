# YourSign web app — local dev. Copy to .env.local; never commit secrets.
NEXT_PUBLIC_SOLANA_CLUSTER=devnet
NEXT_PUBLIC_API_URL=http://127.0.0.1:8787
NEXT_PUBLIC_MCP_URL=http://127.0.0.1:8788

# Privy (ADR-0006). Public app id only — secret stays in apps/api wrangler secret.
NEXT_PUBLIC_PRIVY_APP_ID=
