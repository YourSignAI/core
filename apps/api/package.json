{
  "name": "@yoursign/api",
  "version": "0.1.0",
  "private": true,
  "description": "YourSign API. Hono on Cloudflare Workers. SIWS auth, agent scope persistence, AI Gateway proxy.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint TODO'",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "dependencies": {
    "@yoursign/agent-sdk": "workspace:*",
    "@yoursign/crypto": "workspace:*",
    "@yoursign/schemas": "workspace:*",
    "@yoursign/core-domain": "workspace:*",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
